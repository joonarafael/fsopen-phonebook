{
	"name": "fsopen-phonebook",
	"version": "1.0.0",
	"description": "Phonebook Project for Full Stack open",
	"scripts": {
		"start": "node index.js",
		"dev": "nodemon index.js",
		"build:ui": "rm -rf build && cd ./frontend_src && rm -rf build && yarn && yarn build && cp -r build ../build",
		"test": "\"echo \\\"Error: no test specified\" && exit 1\"",
		"deploy": "yarn && yarn start"
	},
	"author": "joonarafael",
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.4.5",
		"express": "^4.19.2",
		"mongoose": "^8.4.4",
		"morgan": "^1.10.0"
	},
	"devDependencies": {
		"nodemon": "^3.1.4"
	}
}
